import SiteLayout from "../components/SiteLayout";
import StellarSectionPage from "../components/StellarSectionPage";

export default function ForTeamsPage() {
  return (
    <SiteLayout>
      <StellarSectionPage sectionKey="for-teams" />
    </SiteLayout>
  );
}
